extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 Searching for #{searchingBy}
    .search__video
        each video in videos
            +videoBlock({
                title:video.title,
                views:video.views,
                videoFile:video.fildUrl
            })