extends layouts/main
include mixins/videoBlock

block content
    .videos
        each video in fake_videos
            +videoBlock({
                id:video.id,
                title:video.title,
                views:video.views,
                videoFile:video.videoFile
            })